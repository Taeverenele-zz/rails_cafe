<h1>Welcome to Coder Cafe</h1>


<table>
  <tr>
    <th>Table</th>
    <th>Price</th>
  </tr>
  <% @menu.each do |key, value| %>
    <tr>
      <td><%= link_to key.capitalize, order_path(key) %></td>
      <!-- <td>$<%= "%.2f" % value %></td> -->
    </tr>
  <% end %>
</table>